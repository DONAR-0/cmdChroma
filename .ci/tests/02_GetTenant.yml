version: "2"
name: Test Chroma CLI - Get Current Tenant

testcases:
  - name: Current Default_tenant should Exists
    steps:
      - type: exec
        script: cmdChroma --log-level info cT
        assertions:
          - result.code ShouldEqual 0
          - result.systemout ShouldContainSubstring "Checking tenant"
          - result.systemout ShouldContainSubstring "default_tenant"
          - result.systemout ShouldContainSubstring "✅ exists"
  - name: Current Tenant with -tenant flag
    steps:
      - type: exec
        script: cmdChroma --log-level info cT -tenant some_tenant
        assertions:
          - result.code ShouldEqual 0
          - result.systemout ShouldContainSubstring "Checking tenant"
          - result.systemout ShouldContainSubstring "some_tenant"
          - result.systemout ShouldContainSubstring "❌ not found"

  - name: Current Tenant with Env Variable
    steps:
      - type: exec
        script: TENANT=some_tenant cmdChroma --log-level info cT
        assertions:
          - result.code ShouldEqual 0
          - result.systemout ShouldContainSubstring "Checking tenant"
          - result.systemout ShouldContainSubstring "some_tenant"
          - result.systemout ShouldContainSubstring "❌ not found"

